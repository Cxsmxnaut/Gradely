// Data source configuration
export const USE_MOCK = false; // Toggle between mock and real data

// When false, the app will use real backend API calls
// When true, the app will use mock data for development/debugging
